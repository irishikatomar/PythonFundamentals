# Variables
Variables are used to store any Data value(int,float, boolean and etc.) in the memory of computer. 
we can use the stored variable in program referring it by variable name.

Example:

```python
city_name = "Meerut"  # storing data in variable
print(city_name)
```

